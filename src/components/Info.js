import React from 'react';
import Form from "./Form";

const Info = () => {
    return (
        <div>
            <h2>Weather application</h2>
            <p>Your city weather</p>
            <Form/>
        </div>
    );
};

export default Info;